// array

const myArr = [0, 1, 2, 3, 4, 5]
console.log(myArr[0]);

// Array Methods

myArr.push(6)
myArr.pop()
myArr.unshift(9) // Adds 9 to the first position
myArr.shift() // Removes the first element
console.log(myArr);